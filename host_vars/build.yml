---
# Vars for the dedicated build-time identity (see hosts.yml). The image build
# runs limited to this host, so only host-agnostic, build-time config belongs
# here. Machine-specific config (qemu_guest/qemu_host) is intentionally absent
# and applied at firstboot against the real hostname.
#
# These are common defaults. Remaining host-specific build divergence is being
# migrated to firstboot; until then a single shared value lives here.

disk:
  device: /dev/sda

# Superset of modules across hosts so one initramfs boots them all.
mkinitcpio_modules:
  - nvme_core
  - nvme
  - intel_agp
  - i915

modprobe_files:
  - i915.conf
  - iwlwifi.conf
  - nvme.conf

key_repeat:
  rate: 35
  delay: 250

window_manager:
  waybar_minimum_workspace_count: 5
  window_manager: sway
  windowing_system: wayland
