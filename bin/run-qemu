#!/bin/bash

set -o errexit
set -o nounset

source bin/_qemu_common.sh

start_pcscd_vsock_relay
trap stop_pcscd_vsock_relay EXIT INT TERM

qemu-system-x86_64 \
  "${common_qemu_args[@]}" \
  "${gpg_usb_args[@]}" \
  "${system_disk_args[@]}"
