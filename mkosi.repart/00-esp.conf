[Partition]
Type=esp
Format=vfat
CopyFiles=/boot:/
# Hold two UKIs at once: A/B updates keep the running ~460M UKI plus the new one
# (sysupdate InstancesMax=2) during the slot swap, so 1G overflows the FAT ESP.
CopyFiles=/efi:/
SizeMinBytes=2G
