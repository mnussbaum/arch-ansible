[Partition]
Type=usr
Label=_empty
NoAuto=1
# Match usr-a: an inactive slot that only ever receives a ~5G usr image must not
# grow into the disk-wide even split (it was claiming ~22G that root needs for
# build scratch). See 22-usr-a.conf.
SizeMinBytes=8G
SizeMaxBytes=10G
