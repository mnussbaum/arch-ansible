[Partition]
Type=esp
# Format the ESP when newly created (fresh install onto a blank disk); repart
# leaves the existing build ESP untouched on the first-boot grow.
Format=vfat
# Must fit two ~460M UKIs simultaneously (A/B sysupdate InstancesMax=2); 1G is
# too small. Keep in sync with mkosi.repart/00-esp.conf.
SizeMinBytes=2G
