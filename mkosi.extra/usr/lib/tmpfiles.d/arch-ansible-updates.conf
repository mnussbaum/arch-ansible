# Local staging dir for on-device updates. bin/update-system drops the freshly
# built split artifacts here; the /usr/lib/sysupdate.d transfers read them as
# their source, so updatectl can apply them to the inactive A/B slot.
d /var/lib/arch-ansible 0755 root root -
d /var/lib/arch-ansible/updates 0755 root root -
