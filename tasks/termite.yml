# vim:ft=ansible:
---
- name: termite | Install termite
  pacman:
    name: termite
    state: present
  become: true

- name: termite | Create config dir
  file:
    path: "/home/{{ user.name }}/.config/termite"
    owner: "{{ user.name }}"
    state: directory

- name: termite | Clone base16-termite
  git:
    repo: https://github.com/khamer/base16-termite
    dest: "/home/{{ user.name }}/.local/share/base16-termite"

- name: termite | Link base16-termite config
  file:
    src: "/home/{{ user.name }}/.local/share/base16-termite/themes/base16-default-dark.config"
    dest: "/home/{{ user.name }}/.config/termite/config"
    state: link
