# vim:ft=ansible:
---
- name: termite | Install termite
  pacman:
    name: termite
    state: present
  become: true

- name: termite | Create config dir
  file:
    path: "/home/{{ user.name }}/.config/termite"
    owner: "{{ user.name }}"
    state: directory
