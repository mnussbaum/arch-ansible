# vim:ft=yaml.ansible:
---
- name: Install pacman database files
  pacman:
    update_cache: true
    upgrade: true
  retries: 2

- name: Install packages
  package:
    state: present
    name: "{{ bootstrap_packages }}"
