# vim:ft=yaml.ansible:
---
- name: Create config dir
  file:
    path: "{{ user_home_dir }}/.ssh"
    owner: "{{ user.name }}"
    group: "{{ user.group }}"
    state: directory

- name: Install config
  copy:
    owner: "{{ user.name }}"
    group: "{{ user.group }}"
    src: ssh-config
    dest: "{{ user_home_dir }}/.ssh/config"


