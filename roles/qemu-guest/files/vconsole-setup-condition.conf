[Unit]
# The QEMU console doesn't support the font/keymap ioctls, so
# systemd-vconsole-setup fails (KD_FONT_OPT_GET: Input/output error). We boot
# straight into sway here, so skip it under virtualization.
ConditionVirtualization=!vm
